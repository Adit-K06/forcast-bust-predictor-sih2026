# P1 — Day 1

- Scaffolded the React/Vite frontend and established the dashboard component structure (Header, ForecastControls, MapView, RegionPanel, ConfidenceLegend).
- Added the Leaflet map foundation centered on India with working pan/zoom, alongside the forecast date, lead-day slider (Day 1–10), and region selector controls.
- Built the Region Intelligence panel as a polished empty state, structured to receive future backend fields (bust probability, confidence, explanation, contributing factors), plus the conceptual Bust Risk legend and Day-1 wireframe.

# P1 — Day 2

- Rendered real Indian state boundaries on the Leaflet map (geohacker/india dataset, simplified with mapshaper) with hover and click interaction.
- Connected the dashboard to P5's live `/forecast-confidence` endpoint via a dedicated API helper, driven by the existing forecast date, lead-day slider, and a region selector now listing the backend's six actual IMD-subdivision slugs.
- Region Intelligence now displays live bust probability, confidence score, explanation, and top factors (with a mock-data indicator), plus loading and error states; map clicks on states that don't correspond to a backend subdivision surface a clear "data unavailable" message instead of sending an invented slug.