# P5 — Day 1-2

- Scaffolded the FastAPI backend with a `/forecast-confidence` endpoint and
  interactive Swagger docs at `/docs`.
- Defined and documented the API contract (region, date, lead_day →
  bust_probability, confidence_score, explanation_text, top_factors) and
  shared it with P1 (frontend) and P4 (ML) so everyone builds against the same
  shape.
- Extended the endpoint to return realistic, varied mock data across multiple
  region/lead-day combinations, with input validation (unknown regions →
  422, lead_day restricted to 1-10), so the frontend already shows real
  variation instead of one flat number.
